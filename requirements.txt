# ============================================================
# AI Girlfriend 项目自包含依赖
# 安装: pip install -r requirements.txt
#
# 项目内模块 (skills/xxx) 不需要 pip 安装，PYTHONPATH 自动处理。
# ComfyUI (comfy, folder_paths, nodes) 和 GPT-SoVITS (GPT_SoVITS)
# 是外部推理框架，由 config.yaml 的 comfyui_root / sovits_root 指向。
# torch 版本需匹配 CUDA 驱动 + GPU 架构（RTX 5070 Blackwell 需 >=2.7+cu130）。
# ============================================================

# --- 核心（项目 .py 直接 import）---
numpy>=1.24.0
pyyaml>=6.0
Pillow>=10.0.0
scipy>=1.10.0

# --- shiki_daemon.py ---
pystray>=0.19.0

# --- embedding_server.py ---
flask>=2.0.0

# --- mem0_bridge（向量记忆）---
# 运行时可选，不安装则记忆搜索功能降级
# qdrant-client>=1.7.0
# sentence-transformers>=2.2.0

# --- 以下由外部环境提供，不通过 pip 安装 ---
# torch (ComfyUI 自带的 python 环境已包含)
# GPT_SoVITS (sovits_root 指向外部目录)
# ComfyUI (comfyui_root / skills/comfyui_core/)
